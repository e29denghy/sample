<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>{{ route('home') }}</loc></url><url><loc>{{ route('articles.index') }}</loc></url><url><loc>{{ route('projects.index') }}</loc></url>@foreach($articles as $article)<url><loc>{{ $article->canonicalUrl() }}</loc><lastmod>{{ $article->published_at->toAtomString() }}</lastmod></url>@endforeach @foreach($projects as $project)<url><loc>{{ route('projects.show', $project->slug) }}</loc><lastmod>{{ $project->published_at->toAtomString() }}</lastmod></url>@endforeach</urlset>
